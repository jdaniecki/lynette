# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Go

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  verify:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.21'

<<<<<<< HEAD
    - name: Set up Mage
      uses: magefile/mage-action@v2
      with:
        install-only: true

    - name: Build
      run: mage -v build

    - name: Test
      run: mage -v test
=======
    - name: Build
      run: go build -v -o ./build/  cmd/lynette/lynette.go

    - name: Test
      run: go test -v ./...
>>>>>>> 0b3bb00 (CI pipeline)
